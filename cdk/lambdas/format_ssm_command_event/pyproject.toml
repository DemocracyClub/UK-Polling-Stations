[project]
name = "format-ssm-command-event"
version = "0.0.0"
requires-python = "==3.12.*"
dependencies = ["sentry-sdk"]

[dependency-groups]
testing = [
    "moto[sns,sqs]==5.0.22",
]

[tool.uv]
package = false
