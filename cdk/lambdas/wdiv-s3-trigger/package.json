{
  "name": "wdiv-s3-trigger",
  "version": "0.1.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy:dev": "sls deploy --stage dev",
    "logs:dev": "sls logs --stage dev -f lambda_handler",
    "deploy:prod": "sls deploy --stage prod",
    "logs:prod": "sls logs --stage prod -f lambda_handler"
  },
  "author": "chris48s",
  "devDependencies": {
    "serverless": "^1.53.0",
    "serverless-python-requirements": "^5.0.0"
  }
}
